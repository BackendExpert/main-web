# Data Count up in Home menu

- all done
- use enableScrollSpy={true} for coutdown start only when scroll to the component


```html

<p className="text-2xl font-bold"><UpCount enableScrollSpy={true} end={countinfo.coutdata} /></p>


```